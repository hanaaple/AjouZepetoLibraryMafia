export enum MafiaPlayerState {
  None = -1,
  Ready = 1,
  Play = 2,
}

export enum JobState {
  None = -2,
  Citizen = 1,
  Mafia = 2,
}

export enum InGameInteractState {
  ALIVE = 1,
  GHOST = 2,
  CORPSE = 3,
  MISSION = 4,
}
